# Claude Code permission deny rules that the installer adds to .claude/settings.json (since v3.9.0).
# They stop Claude Code's file tools from opening files that normally hold API keys or access tokens.
# The programs that use these files still load them; only the agent is kept out.
# To let Claude read one of these files, remove its rule from permissions.deny in .claude/settings.json.
Read(.env)
Read(.env.local)
Read(.env.*.local)
Read(.env.production)
Read(.mcp.json)
Read(.codex/config.toml)
